# Queries

There are some example queries to the KB in this folder.
`.rq` and `.sparql` are file extension conventions for your queries.

Add your own queries here to easily run them with the [query shell script](../query_kb.sh).
